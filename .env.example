# Port for the reverse proxy server
PORT=3001

# Secret key used for signing JWT auth tokens
JWT_SECRET=super-secret-scratchers-key

# Access password for the login page
PASSWORD=passforscratchers

# Fallback proxy target for _h5ai and generic requests
UPSTREAM_TARGET=http://172.16.50.14
